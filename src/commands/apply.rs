pub fn run(custom: Option<String>, only_direct: bool, only_inverse: bool, update_features: bool, prefer_code: bool, prefer_specs: bool, gen_temperature: Option<f64>) -> Result<(), Box<dyn std::error::Error>> {
    let _ = (custom, only_direct, only_inverse, update_features, prefer_code, prefer_specs, gen_temperature);
    println!("apply: not yet implemented");
    Ok(())
}
